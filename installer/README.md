# Installer

This folder is reserved for:

- Inno Setup (or NSIS) scripts
- Code signing instructions
- Build outputs (do not commit final binaries to git; publish them to GitHub Releases)

Recommended behavior:

- Install binaries to `C:\Program Files\GodTierBot\app\`
- Persist runtime data in `C:\ProgramData\GodTierBot\`
